# SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Keep only the canonical JSONL export plus config helpers in git
*.jsonl
!issues.jsonl
beads*.meta.json
!metadata.json
!config.json
