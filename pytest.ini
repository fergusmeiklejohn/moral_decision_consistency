[pytest]
testpaths = tests
addopts = --cov=src/config --cov=src/dilemmas --cov=src/data --cov=src/analysis/metrics.py --cov-report=term-missing --cov-fail-under=80
markers =
    unit: fast, isolated unit tests
    integration: slower tests that exercise multiple components together
